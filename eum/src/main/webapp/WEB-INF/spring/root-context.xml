<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xsi:schemaLocation="
         http://www.springframework.org/schema/beans   http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
         http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd
         http://www.springframework.org/schema/aop     http://www.springframework.org/schema/aop/spring-aop-3.1.xsd
         http://www.springframework.org/schema/tx      http://www.springframework.org/schema/tx/spring-tx-3.1.xsd">

  <!-- AOP -->
  <aop:aspectj-autoproxy />

  <!-- (Root 컨텍스트) Service/DAO만 스캔: Controller는 servlet-context에서 스캔 -->
  <context:component-scan base-package="com.myspring.eum">
    <context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller"/>
  </context:component-scan>

  <!-- DataSource: Java 6 호환 DBCP + MySQL 5.1.x 드라이버(com.mysql.jdbc.Driver) -->
  <bean id="dataSource" class="org.apache.commons.dbcp.BasicDataSource" destroy-method="close">
    <property name="driverClassName" value="com.mysql.jdbc.Driver"/>
    <!-- DB명, 계정/비번 바꿔주세요 -->
    <property name="url" value="jdbc:mysql://localhost:3306/eum?useUnicode=true&amp;characterEncoding=UTF-8&amp;autoReconnect=true&amp;useSSL=false"/>
    <property name="username" value="root"/>
    <property name="password" value="비밀번호"/>

    <!-- 커넥션 풀 옵션(필요 시 조정) -->
    <property name="maxActive" value="20"/>
    <property name="maxIdle" value="10"/>
    <property name="maxWait" value="10000"/>
    <property name="validationQuery" value="SELECT 1"/>
    <property name="testOnBorrow" value="true"/>
  </bean>

  <!-- MyBatis SqlSessionFactory -->
  <bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
    <property name="dataSource" ref="dataSource"/>
    <!-- 매퍼 XML 위치: 프로젝트 구조에 맞게 경로 조정 -->
    <!-- 예) /WEB-INF/mybatis/*.xml 로 둘 경우: value="classpath*:/mybatis/*.xml" 대신 "/WEB-INF/mybatis/*.xml" -->
    <property name="mapperLocations" value="classpath*:/mybatis/*.xml"/>
    <!-- 선택: VO 패키지 별칭 -->
    <!-- <property name="typeAliasesPackage" value="com.myspring.eum.member.vo,com.myspring.eum.admin.vo"/> -->
  </bean>

  <!-- SqlSessionTemplate (DAO/Service에서 @Autowired SqlSession 사용 가능) -->
  <bean id="sqlSession" class="org.mybatis.spring.SqlSessionTemplate">
    <constructor-arg index="0" ref="sqlSessionFactory"/>
  </bean>

  <!-- 트랜잭션 -->
  <bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
    <property name="dataSource" ref="dataSource"/>
  </bean>
  <tx:annotation-driven transaction-manager="transactionManager"/>

</beans>
