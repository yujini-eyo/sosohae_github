<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.adminAuth">

  <!-- AdminVO 매핑 -->
  <resultMap id="AdminMap" type="com.myspring.eum.admin.vo.AdminVO">
    <id     property="id"        column="id"/>
    <result property="password"  column="password"/>
    <result property="name"      column="name"/>
    <result property="email"     column="email"/>
    <result property="address"   column="address"/>
    <result property="age"       column="age"/>
    <result property="birth"     column="birth"/>
    <result property="phone"     column="phone"/>
    <result property="code"      column="code"/>
    <result property="notes"     column="notes"/>
    <result property="createdAt" column="created_at"/>
  </resultMap>

  <!-- [기본] admin 테이블에서 관리자 1명 조회 -->
  <select id="findAdminById" parameterType="string" resultMap="AdminMap">
    SELECT *
    FROM admin
    WHERE id = #{id}
    LIMIT 1
  </select>

  <!-- [대안: 주석 해제시 사용] member 테이블에서 role='ADMIN'만 관리자라고 보는 경우 -->
  <!--
  <select id="findAdminById" parameterType="string" resultMap="AdminMap">
    SELECT *
    FROM member
    WHERE id = #{id}
      AND role = 'ADMIN'
    LIMIT 1
  </select>
  -->

</mapper>
