<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member">

	<resultMap id="memResult"
		type="com.myspring.eum.member.vo.MemberVO">
		<id property="id" column="id" />
		<result property="password" column="password" />
		<result property="name" column="name" />
		<result property="email" column="email" />
		<result property="address" column="address" />
		<result property="age" column="age" />
		<result property="birth" column="birth" />
		<result property="phone" column="phone" />
		<result property="code" column="code" />
		<result property="notes" column="notes" />
		<result property="createdAt" column="created_at" />
	</resultMap>

	<select id="findById" parameterType="string"
		resultMap="memResult">
		SELECT *
		FROM member
		WHERE id = #{id}
		LIMIT 1
	</select>

	<select id="selectAllMemberList" resultMap="memResult">
		SELECT * FROM member ORDER BY created_at DESC
	</select>

	<insert id="insertMember"
		parameterType="com.myspring.eum.member.vo.MemberVO">
		INSERT INTO member (id, password, name, email, address, age, birth, phone,
		code, notes)
		VALUES (#{id}, #{password}, #{name}, #{email}, #{address}, #{age}, #{birth},
		#{phone}, #{code}, #{notes})
	</insert>


	<!-- 비밀번호 null/빈문자면 SET하지 않음 -->
	<update id="updateMember"
		parameterType="com.myspring.eum.member.vo.MemberVO">
		UPDATE member
		<set>
			<if test="password != null and password != ''">password = #{password},</if>
			<if test="name    != null">name = #{name},</if>
			<if test="email   != null">email = #{email},</if>
			<if test="address != null">address = #{address},</if>
			<if test="age     != null">age = #{age},</if>
			<if test="birth   != null">birth = #{birth},</if>
			<if test="phone   != null">phone = #{phone},</if>
			<if test="code    != null">code = #{code},</if>
			<if test="notes   != null">notes = #{notes},</if>
		</set>
		WHERE id = #{id}
	</update>

	<delete id="deleteMember" parameterType="string">
		DELETE FROM member
		WHERE id = #{_parameter}
	</delete>

	<select id="loginById"
		parameterType="com.myspring.eum.member.vo.MemberVO"
		resultMap="memResult">
		SELECT *
		FROM member
		WHERE id = #{id}
		AND password = #{password}
		LIMIT 1
	</select>

</mapper>
